const navbarToggle = document.body.querySelector('.navbar-toggler');
const navbarElements = document.body.getElementByClassName('.collapse')

toggleNav = () => {
    console.log(`button clicked`)
    navbarElements.st
}

navbarToggle.addEventListener('click',toggleNav())